namespace bootstrap

use compiler.cli.*

/// Seed entry until level-1b owns parser, semantic checker, and WAT emission.
def main(): i64 =
    if cli_ready_for_codegen(cli_default_config()) { 0 } else { 1 }
