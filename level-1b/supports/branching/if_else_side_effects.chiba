namespace level1b.branching.if_else_side_effects

def then_effect(x: i64): i64 = x + 1

def else_effect(x: i64): i64 = x - 1

def choose(flag: bool, value: i64): i64 =
    if flag {
        then_effect(value)
    } else {
        else_effect(value)
    }
