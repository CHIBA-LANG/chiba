# level-1b tools

This directory is for tools written in level-1b Chiba that should eventually be
compiler subcommands or internal build helpers.

Repository-level Node scripts under `tools/node` are allowed as CI/development
harnesses, but level-1b tools must not rely on Node-only runtime semantics.
